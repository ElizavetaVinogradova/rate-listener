1. [done]Установить соединение с биржей криптовалют CoinBase через WebSocket (документация: https://docs.cloud.coinbase.com/exchange/docs/websocket-overview)
2. [done]Подписаться на обновления котировок по трём инструментам: ETH-BTC, BTC-USD, BTC-EUR. Данные будут поступать через WebSocket по мере их появления на бирже.
3. [done]Создать таблицу "ticks" в MySQL со следующими полями: 
    timestamp (BIGINT) - Unixtime в миллисекундах, 
    symbol (VARCHAR) - название инструментов, 
    best_bid (DOUBLE) - цена предложения продажи, 
    best_ask (DOUBLE) - цена предложения покупки.
4. [done]Установить соединение с MySQL и записывать в таблицу "ticks" данные, полученные через WebSocket. 
Необходимо предусмотреть запись данных пакетами (батчами), а также повторные попытки (ретраи) в случае ошибки записи. 
Размер пакета, таймаут и максимальное количество попыток должны быть конфигурируемыми

5. [done]Добавить Кафку, чтобы писать в нее тики, и читать их из Кафки в базу.
5.1 [done]Разделить main чтобы читатель и писатель Кафки были в разных бинарниках. Это важно для масштабируемости. 

6. Добавить API 
6.1 с grpc 
6.2 c http

7. Покрыть тестами
8. Добавить graceful shutdown
9. Добавить метрики
10. Добавить линтеры
11. Добавить Redis


-------------------------
Бэклог

1. broker := kafka.NewBrokerReader([]string{"localhost:9093"}, "ticks") вынести IP и название топика в конфиг
2. panic(fmt.Sprintf("Couldnt initialize the config file: %s", err))
Лучше через логгер, чтобы инфа отобразилась нормально в логах. Посмотреть у логруса метод типа Fatalf(), который пишет в лог и завершает работу приложения. 
3. url := viper.GetString("coinBaseClient.url")
Сделать структуру под конфиг, в которую конфиг будет парсится. Изучить, как это делать.